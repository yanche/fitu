
var SingleSession = require('./singlesession.js');
var Sessions = require('./sessions.js');

var sessionsResFactory = function (webreq) {
    if (webreq.search.id)
        return new SingleSession(webreq);
    else
        return new Sessions(webreq);
};

module.exports = sessionsResFactory;
