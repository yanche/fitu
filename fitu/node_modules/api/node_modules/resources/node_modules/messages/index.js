
var Messages = require('./messages.js');
var SingleMessage = require('./singlemessage.js');

var messagesResFactory = function (webreq) {
    if (webreq.search.id)
        return new SingleMessage(webreq);
    else
        return new Messages(webreq);
};

module.exports = messagesResFactory;
