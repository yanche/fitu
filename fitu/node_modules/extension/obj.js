
var validate = require('validate');

var len = function (obj) {
    if (Array.isArray(obj))
        return obj.length;
    else if (validate.isObj(obj)) {
        var l = 0;
        for (var i in obj)
            ++l;
        return l;
    }
    else
        return 0;
};

exports.objLen = len;
